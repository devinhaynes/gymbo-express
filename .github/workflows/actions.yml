name: Deploy Code

on:
  push:
    branches:
      - master

jobs:
  build:
    name: Test build
    runs-on: ubuntu-latest
    steps:
      - name: Run some commands
        run: |
          ls -a
          pwd
          echo "Hello World"
          docker version
